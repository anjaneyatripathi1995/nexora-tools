<IfModule mod_rewrite.c>
    RewriteEngine On

    # If document root is project root (e.g. some shared hosts), send all requests to public/
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
