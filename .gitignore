# Dependencies
/vendor
/node_modules

# Environment & secrets
.env
.env.backup
.env.production
.env.*.local

# Laravel build & runtime
/public/hot
/public/storage
# Note: public/build IS committed so Hostinger (no Node.js) can serve compiled assets
/storage/*.key
/storage/pail
/bootstrap/cache/*.php

# Logs & cache
*.log
.phpunit.result.cache
.phpunit.cache

# IDE & OS
.DS_Store
Thumbs.db
/.fleet
/.idea
/.nova
/.vscode
/.zed
.phpactor.json

# Legacy backup (remove from repo after migration)
/_old_app

# Optional
Homestead.json
Homestead.yaml
auth.json
